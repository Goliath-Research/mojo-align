"""methylGrapher-mojo Python engine.

Faithful port of methylGrapher 0.2.0 (twlab/methylGrapher), adapted to run
as a package (``python -m engine.cli``) and as the interop target for the
Mojo CLI (``src/main.mojo``). See ``MIGRATION_LOG.md`` for the list of
behavioral patches applied on top of the original 0.2.0 sources.
"""

__version__ = "0.2.0"
